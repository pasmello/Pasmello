{
  "name": "pasmello",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @pasmello/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "check": "pnpm -r check"
  },
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  }
}
